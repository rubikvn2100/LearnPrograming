# Note: In Python, we can add an element to the end of a list using the append method:
#         Ex:
#           my_list = [0, 1, 2]
#           my_list.append(3)
#           print(my_list) # displays [0, 1, 2, 3]
#
# Create a function that takes an integer N as input.
# The function should return a list of integers from 0 to N - 1 in ascending order.
# If N is non-positive, return an empty list.
# Ex:
#   createListOfInteger(0) # return []
#   createListOfInteger(4) # return [0, 1, 2, 3]
